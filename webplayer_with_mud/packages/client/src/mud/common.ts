
/* Autogenerated file. Do not edit manually. */
export enum SelectorType {
    None,
    SelectTarget,
    SelectorCard,
    SelectorChoice
}

export enum RarityType {
    COMMON,
    UNCOMMON,
    RARE,
    MYTHIC
}

export enum PackType {
    FIXED,
    RANDOM
}

export enum TeamType {
    FIRE,
    FOREST,
    WATER,
    NEUTRAL
}

export enum GameType {
    SOLO,
    ADVENTURE,
    MULTIPLAYER,
    HOST_P2P,
    OBSERVER
}

export enum GameMode {
    CASUAL,
    RANKED
}

export enum GameState {
    INIT,
    PLAY,
    GAME_ENDED
}

export enum GamePhase {
    NONE,
    START_TURN,
    MAIN,
    END_TURN
}

export enum CardType {
    None,
    Hero,
    Character,
    Spell,
    Artifact,
    Secret,
    Equipment
}

export enum CardTeam {
    None,
    Green,
    Red,
    Blue
}

export enum CardTrait {
    None,
    Wolf,
    Dragon,
    Growth,
    SpellDamage
}

export enum AbilityTrigger {
    NONE,
    ONGOING,
    ACTIVATE,
    ON_PLAY,
    ON_PLAY_OTHER,
    START_OF_TURN,
    END_OF_TURN,
    ON_BEFORE_ATTACK,
    ON_AFTER_ATTACK,
    ON_BEFORE_DEFEND,
    ON_AFTER_DEFEND,
    ON_KILL,
    ON_DEATH,
    ON_DEATH_OTHER
}

export enum AbilityTarget {
    None,
    Self,
    PlayerSelf,
    PlayerOpponent,
    AllPlayers,
    AllCardsBoard,
    AllCardsHand,
    AllCardsAllPiles,
    AllSlots,
    AllCardData,
    PlayTarget,
    AbilityTriggerer,
    EquippedCard,
    SelectTarget,
    CardSelector,
    ChoiceSelector,
    LastPlayed,
    LastTargeted,
    LastDestroyed,
    LastSummoned
}

export enum Team {
    FIRE,
    FOREST,
    WATER,
    NEUTRAL
}

export enum Action {
    PlayCard,
    Attack,
    AttackPlayer,
    Move,
    CastAbility,
    SelectCard,
    SelectPlayer,
    SelectSlot,
    SelectChoice,
    CancelSelect,
    EndTurn,
    ChangeMana,
    AddStatus,
    ChangeCard
}

export enum PileType {
    None,
    Board,
    Hand,
    Deck,
    Discard,
    Secret,
    Equipped,
    Temp
}

export enum EffectStatType {
    None,
    Attack,
    HP,
    Mana
}

export enum ConditionObjType {
    ConditionCardType
}

export enum EffectAttackerType {
    Self,
    AbilityTriggerer,
    LastPlayed,
    LastTargeted
}

export enum Status {
    None,
    Armor,
    Attack,
    Deathtouch,
    Flying,
    Fury,
    Hp,
    Intimidate,
    Invincibility,
    Lifesteal,
    Paralysed,
    Poisoned,
    Protected,
    Shell,
    Silenced,
    Sleep,
    SpellImmunity,
    Stealth,
    Taunt,
    Trample,
    Protection
}

export enum ConditionStatType {
    None,
    Attack,
    HP,
    Mana
}

export enum ConditionPlayerType {
    Self,
    Opponent,
    Both
}

export enum ConditionOperatorInt {
    Equal,
    NotEqual,
    GreaterEqual,
    LessEqual,
    Greater,
    Less
}

export enum ConditionOperatorBool {
    IsTrue,
    IsFalse
}

export enum ConditionTargetType {
    None,
    Card,
    Player,
    Slot,
    CardData
}
